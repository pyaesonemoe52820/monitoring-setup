global:
  scrape_interval: 15s   # How often to scrape targets

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['prometheus:9090']

  - job_name: 'python-app'
    static_configs:
      - targets: ['app:8000']   # We'll add /metrics endpoint in your Python app